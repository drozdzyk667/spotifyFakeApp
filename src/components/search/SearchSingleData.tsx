import React from 'react';

interface SearchProps {
  data: any[];
}

const SearchSingleData: React.FC<SearchProps> = ({ data }) => {
  return <div>{'lolool'}</div>;
};

export default SearchSingleData;
